meta {
  name: Schedule
  type: http
  seq: 3
}

post {
  url: https://{{host.lunches.api}}/
  body: json
  auth: inherit
}

body:json {
  {
    "schoolId": "{{schoolId}}",
    "restaurantId": "{{restaurantId}}",
    "date": "2025-08-20"
  }
}

vars:post-response {
  lunchId: res.body.lunchId
}
